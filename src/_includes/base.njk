<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | WISeAgent Blogs</title>
    <link rel="stylesheet" href="{{ '/assets/style.css' | url }}">
</head>
<body>
    <header>
        <div class="container">
            <h1><a href="{{ '/' | url }}">WISeAgent Blogs</a></h1>
            {% if parent %}
                <nav>
                    <a href="{{ parent | url }}">{{ parentTitle }}</a>
                </nav>
            {% endif %}
        </div>
    </header>

    <div class="container">
        <main>
            {{ content | safe }}
        </main>
    </div>

    <footer class="container">
        <p>&copy; {{ page.date.getFullYear() }} WISeAgent. Hosted with GitHub Pages.</p>
    </footer>
</body>
</html>