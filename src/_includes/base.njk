<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title or "WISeAgent Blogs" }}</title>
    <link rel="stylesheet" href="{{ '/assets/style.css' | url }}">
</head>
<body>
    <header>
        <h1><a href="{{ '/' | url }}">WISeAgent Blogs</a></h1>
        {% if parent %}<a href="{{ parent | url }}">Back to {{ parentTitle }}</a>{% endif %}
    </header>
    <main>
        {{ content | safe }}
    </main>
    <footer class="mt-4">
        <p>&copy; 2025 WISeAgent. Hosted with GitHub Pages.</p>
    </footer>
</body>
</html>